{
  "name": "colors",
  "version": "1.0.0",
  "description": "",
  "main": "",
  "scripts": {
    "stylus": "node_modules/stylus/bin/stylus -w src/public/stylus/main.styl -o src/public/css/main.css",
    "uglify": "mkdir -p web/dist/js && uglifyjs src/public/js/*.js -m -o src/public/dist/js/main.js",
    "autoprefixer": "postcss -u autoprefixer -r src/public/css/*.css",
    "imagemin": "imagemin src/public/images src/public/dist/images -p",
    "lint:js": "./node_modules/.bin/eslint web/src/js",
    "build:css": "npm run stylus; npm run autoprefixer",
    "watch:css": "onchange 'src/public/stylus/*.styl' -- npm run build:css",
    "watch:js": "onchange 'src/public/js/main.js' -- npm run build:js",
    "build:js": "browserify src/public/js/main.js -o src/public/js/bundle.js",
    "docker:up": "docker-compose build && docker-compose up -d",
    "docker:down": "docker-compose down",
    "start": "node app.js",
    "build": "npm run stylus; npm run autoprefixer; npm run build:js",
  },
  "author": "Simon Breiter",
  "license": "MIT",
  "devDependencies": {
    "browserify": "^13.1.0",
    "imagemin-cli": "^3.0.0",
    "node-inspector": "^0.12.8",
    "nodemon": "^1.10.0",
    "onchange": "^2.5.0",
    "uglify-js": "^2.7.0"
  },
  "dependencies": {
    "autoprefixer": "^6.3.7",
    "express": "^4.14.0",
    "jquery": "^3.1.0",
    "postcss-cli": "^2.5.2",
    "pug": "^2.0.0-beta4",
    "stylus": "^0.54.5"
  }
}
